"main";let merge;_c92‍.w("lodash.merge",[["default",["merge"],function(v){merge=v}]]);let devConfig;_c92‍.w("./dev",[["default",["devConfig"],function(v){devConfig=v}]]);let prodConfig;_c92‍.w("./prod",[["default",["prodConfig"],function(v){prodConfig=v}]]);



process.env.NODE_ENV = process.env.NODE_ENV || 'development'
const env = process.env.NODE_ENV


const baseConfig = {
    secret: {}
}

let envConfig = {}

switch (env) {
    case 'development':
    case 'dev':
        envConfig = devConfig
        break
    case 'production':
    case 'prod':
        envConfig = prodConfig
        break

    default:
        envConfig = devConfig
}
_c92‍.d(merge(baseConfig, envConfig));

