"main";let express;_b93‍.w("express",[["default",["express"],function(v){express=v}]]);let dbConnect;_b93‍.w("./database",[["dbConnect",["dbConnect"],function(v){dbConnect=v}]]);
// import bodyParser from 'body-parser';


const app = express();
dbConnect()
app.set("port", 4000);

const server = app.listen(app.get("port"), () => {
  _b93‍.g.console.log(`App is running on port ${server.address().port}`);
});

// ESM syntax is supported.

