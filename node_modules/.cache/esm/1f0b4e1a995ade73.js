"main";let express;_995‍.w("express",[["default",["express"],function(v){express=v}]]);let dbConnect;_995‍.w("./database",[["default",["dbConnect"],function(v){dbConnect=v}]]);
// import bodyParser from 'body-parser';


const app = express();
dbConnect()
app.set("port", 4000);

const server = app.listen(app.get("port"), () => {
  _995‍.g.console.log(`App is running on port ${server.address().port}`);
});

// ESM syntax is supported.

